/**
 * Neutral {@link net.minecraft.world.level.levelgen.feature.Feature} primitives — geometric
 * placement patterns that consumers reference from {@code configured_feature} JSON via
 * {@code "type": "isekai_api:<name>"}. Each feature is theme-agnostic: blocks / fluids are
 * codec fields supplied by the consumer, never baked. See
 * {@link com.kuronami.isekaiapi.feature.IsekaiFeatures} for the registry.
 */
package com.kuronami.isekaiapi.feature;
